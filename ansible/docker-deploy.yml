---
- name: Install Docker Engine on Ubuntu 24.04
  hosts: all
  vars:
    deploy_user: "{{ lookup('env', 'DEPLOY_USER') | default('deploy') }}"
    docker_arch: "amd64"         # adjust if using arm64
    docker_ubuntu_release: "noble"

  tasks:
    # ------------------------------
    # Remove old Docker packages
    # ------------------------------
    - name: Remove old/conflicting Docker packages
      apt:
        name:
          - docker
          - docker-engine
          - docker.io
          - containerd
          - runc
        state: absent
        purge: yes
        update_cache: yes

    # ------------------------------
    # Install prerequisites
    # ------------------------------
    - name: Install prerequisites for Docker
      apt:
        name:
          - apt-transport-https
          - ca-certificates
          - curl
          - gnupg
          - lsb-release
        state: present
        update_cache: yes

    # ------------------------------
    # Add Docker GPG key
    # ------------------------------
    - name: Add Docker GPG key
      shell: |
        install -m 0755 -d /etc/apt/keyrings
        curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
      args:
        creates: /etc/apt/keyrings/docker.gpg

    # ------------------------------
    # Remove broken docker.list if exists
    # ------------------------------
    - name: Remove broken docker.list if present
      file:
        path: /etc/apt/sources.list.d/docker.list
        state: absent

    # ------------------------------
    # Add Docker repository (single line, valid)
    # ------------------------------
    - name: Add Docker repository for Ubuntu 24.04
      copy:
        dest: /etc/apt/sources.list.d/docker.list
        content: "deb [arch={{ docker_arch }} signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu {{ docker_ubuntu_release }} stable"

    # ------------------------------
    # Update apt cache
    # ------------------------------
    - name: Update apt cache
      apt:
        update_cache: yes

    # ------------------------------
    # Install Docker packages
    # ------------------------------
    - name: Install Docker Engine packages
      apt:
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
          - docker-buildx-plugin
          - docker-compose-plugin
        state: present
        update_cache: yes

    # ------------------------------
    # Ensure Docker service is running
    # ------------------------------
    - name: Ensure Docker service is running
      systemd:
        name: docker
        enabled: yes
        state: started

    # ------------------------------
    # Ensure docker group exists and add deploy user
    # ------------------------------
    - name: Ensure docker group exists
      group:
        name: docker
        state: present

    - name: Add deploy user to docker group
      user:
        name: "{{ deploy_user }}"
        groups: docker
        append: yes
